name: libreoffice_ubuntu
description: Install LibreOffice suite on Ubuntu

steps:
  - name: install_libreoffice
    script: |
      source "$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)/lib/utils/log_verbose.sh"
      log_verbose "📦 Installing..."
      sudo apt update -y
      sudo apt install -y libreoffice
      echo "✅ LibreOffice installed!"
