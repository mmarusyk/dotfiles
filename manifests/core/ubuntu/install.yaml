name: core_ubuntu_install
description: Install base dependencies for Ubuntu/Debian

steps:
  - name: update_system
    install: sudo apt update -y && sudo apt upgrade -y

  - name: install_basics
    install: sudo apt install -y build-essential git curl yq
