name: screenshot_arch
description: Install region screenshot utilities for Wayland/Hyprland on Arch Linux

steps:
  - name: install_screenshot_tools
    script: |
      sudo pacman -S --noconfirm --needed grim slurp wl-clipboard

  - name: create_screenshots_dir
    script: |
      mkdir -p "$HOME/Pictures/Screenshots"
