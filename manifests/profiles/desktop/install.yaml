name: desktop_profile
description: Desktop profile — dev + Hyprland WM + GUI apps + terminal multiplexer

steps:
  - name: dev
    manifest: profiles/dev

  - name: install_zellij
    manifest: zellij

  - name: configure_ptyxis
    manifest: ptyxis

  - name: install_chrome
    manifest: chrome

  - name: install_obsidian
    manifest: obsidian

  - name: install_gimp
    manifest: gimp

  - name: install_libreoffice
    manifest: libreoffice

  - name: install_vlc
    manifest: vlc
