name: zellij_ubuntu_update
description: Update Zellij to the latest version

steps:
  - name: update_zellij
    script: |
      ARCH=$(uname -m)
      VERSION=$(curl -s https://api.github.com/repos/zellij-org/zellij/releases/latest \
        | grep '"tag_name"' | sed 's/.*"tag_name": "\(.*\)".*/\1/')
      CURRENT=$(zellij --version 2>/dev/null | awk '{print $2}')
      if [[ "v${CURRENT}" == "${VERSION}" ]]; then
        log_info "✅ Zellij is already up to date (${VERSION})"
        exit 0
      fi
      log_info "⬆️  Updating Zellij ${CURRENT} → ${VERSION}..."
      curl -L "https://github.com/zellij-org/zellij/releases/download/${VERSION}/zellij-${ARCH}-unknown-linux-musl.tar.gz" \
        -o /tmp/zellij.tar.gz
      tar -xzf /tmp/zellij.tar.gz -C /tmp
      sudo mv /tmp/zellij /usr/local/bin/zellij
      rm /tmp/zellij.tar.gz
